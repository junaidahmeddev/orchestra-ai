// Placeholder — real form + NextAuth wiring happens in Phase 2 (Authentication).
export default function LoginPage() {
  return (
    <main className="flex min-h-screen items-center justify-center">
      <p className="text-muted-foreground">Login page — built in Phase 2.</p>
    </main>
  );
}
