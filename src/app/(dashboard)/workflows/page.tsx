// Placeholder — workflow list + "New Workflow" CTA built in Phase 3 (Workflow CRUD).
export default function WorkflowsListPage() {
  return (
    <main className="p-8">
      <p className="text-muted-foreground">Workflows dashboard — built in Phase 3.</p>
    </main>
  );
}
